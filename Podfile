# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'ImagesDownloadAndCache' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ImagesDownloadAndCache
  pod 'SwiftyJSON', '~> 4.0'
  pod 'SDWebImage', '~> 5.0'

  target 'ImagesDownloadAndCacheTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImagesDownloadAndCacheUITests' do
    # Pods for testing
  end

end
